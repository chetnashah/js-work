/**
 * Throttle wrapFn to be called only once per delayMs
 * @param {*} wrapFn 
 * @param {*} delayMs 
 * @param {*} options object with leading/trailing
 */
module.exports = function(wrapFn, delayMs, options) {

}